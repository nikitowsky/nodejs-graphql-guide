{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "developer",
  "password": "password",
  "database": "example_db",
  "synchronize": true,
  "logging": true,
  "entities": ["src/entities/**/*.entity.ts"],
  "migrations": ["src/migrations/**/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
